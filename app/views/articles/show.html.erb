<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>
 
<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>

<h2 style="color:grey">Comments</h2>	<!--display existing comments-->
<%= render @article.comments %> <!--ATTENTION !!!! read below -->
<!--This will now render the partial in app/views/comments/_comment.html.erb once for each comment that is in the @article.comments collection. As the render method iterates over the @article.comments collection, it assigns each comment to a local variable named the same as the partial, in this case comment which is then available in the partial for us to show. -->

<h2>Add a comment:</h2>		<!--adding comments to already created post. Comment is a separate model -->
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_article_path(@article) %> <!--This will now render the partial in                      app/views/comments/_form.html.erb. 

<%= link_to 'Back' , articles_path %>  	<!-- returns to articles index -->